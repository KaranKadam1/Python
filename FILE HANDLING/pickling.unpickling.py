#Pickling and Unpickling = serialization and deserialization
# in serialization complex object convert into byte by byte format and stores data with metadata(data about data)
# in python is called pickling
# serialization types=1)binary,2)xml,3)json

# read the serialized data and recreate the object is called deserialization or unpickling

import pickle
